- Actual:
  + Click trực tiếp vào "Xem query mẫu" mở trên cùng 1 tab không có lỗi
  + Click và giữ Ctrl(Window) để mở trong 1 tab mới thì lỗi Page Not Found
```
```

- Expect: Khi click vào "Xem query mẫu" thì mở tab mới không lỗi
  